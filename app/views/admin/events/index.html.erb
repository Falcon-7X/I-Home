<h2>事件管理--管理员</h2>

<div class="pull-right">
  <%= link_to("增加一个事件", new_admin_event_path, :class => "btn btn-default" ) %>
</div>

<br><br>

<table class="table table-bordered">
  <% @events.each do |event| %>
  <tr>
    <td>
      <%= render_admin_event_status(event) %>
      <%= link_to(event.title, admin_event_path(event)) %>
    </td>
    <td>
      <%= event.user.email %>
    </td>
    <td>
      <%= link_to("编辑", edit_admin_event_path(event)) %>
      <%= link_to("删除", admin_event_path(event), :method => :delete, :data => { :confirm => "Are you sure?" }) %>

      <% if event.is_hidden %>
        <%= link_to("显示", publish_admin_event_path(event), :method => :post, :class => "btn btn-xs btn-success") %>
      <% else %>
        <%= link_to("隐藏", hide_admin_event_path(event), :method => :post, :class => "btn btn-xs btn-danger") %>
      <% end %>
    </td>
    <td>
      <%= event.created_at %>
    </td>
  </tr>
  <% end %>
</table>
