<h1> 增加事件 </h1>

<%= simple_form_for @event do |f| %>

<%= f.input :title , label: "名称" %>
<%= f.input :description, label: "内容" %>
<%= f.input :endtime, label: "截止时间" %>
<%= f.input :is_hidden, label: "隐藏"  %>

  <%= f.submit "提交", class: "btn btn-danger" %>
<% end %>
